interface:
  display_name: "North Star"
  short_description: "Shape VRD (what/why/feel) into PRD and spec"
  default_prompt: "Use $lc-north-star to capture the VRD (what is desired and why) first, then PRD and spec only as needed. Plan is optional."
policy:
  allow_implicit_invocation: false
