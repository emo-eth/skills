# Session Handoff - 2026-08-11

## Current State

- **Branch:** `wall-clock`; no pull request.
- **Phase:** v0 contract revision and implementation pass.
- **Build/Test status:** `npm test` in `plugins/wall-clock/` passes 22 tests, 0 failures.
- **Delivery:** The current branch is being pushed to `origin/wall-clock` with this handoff.

## What Was Done

- Packaged wall-clock as an Agent Plugins 1.0.0 package under `plugins/wall-clock/` with `plugin.json`, `skills/wall-clock/SKILL.md`, `mcp.json`, and a Node launcher.
- Extended the controller with explicit `block-new` and `abort-running` expiry policies, measured timing, assignment lifecycle, reports, plan revisions, and persisted state.
- Added shared native host integration plus Pi and OMP adapters for lifecycle events, per-turn context, pre-tool decisions, child assignment tracking, and abort observation.
- Added focused controller, host, MCP, and package contract tests; updated the product state and append-only decision documentation.

## Next Steps

1. Run the adapter against real Pi and OMP hosts, not only the fake host in `plugins/wall-clock/tests/host.test.ts`.
2. Prove the reviewed native contract end to end: measured context on every turn, fail-closed activation, `block-new`, `abort-running` with an observed abort signal, and child elapsed-time reporting.
3. Add real-host end-to-end tests under `plugins/wall-clock/tests/` before describing the package as complete.
4. Revisit Codex and Claude activation only after an open, tested enforcement seam exists; keep MCP deadline checks guidance-only.

## Decisions Made

- Keep the portable Agent Plugins package separate from `npx skills`; use `plugins/wall-clock/` for discovery and native Pi/OMP loading.
- Require an explicit user-selected expiry policy and reject activation when the host cannot enforce it.
- Treat budgets as ceilings, not targets. Finish assignments as soon as their acceptance target is met.
- Never report cancellation unless the host executor confirms that the action stopped.

## Known Issues

- [ ] Real Pi and OMP runtime enforcement is not yet proven against the reviewed contract.
- [ ] MCP tools do not intercept unrelated client tools and therefore provide model guidance unless a client supplies a native pre-tool gate.
- [ ] Provider-specific remote cancellation and Claude proprietary enforcement remain deferred.

## Context

- Main source and tests: `plugins/wall-clock/src/` and `plugins/wall-clock/tests/`.
- Product contract: `docs/prds/2026-08-11-wall-clock/prd.md`.
- Capability boundary: `docs/prds/2026-08-11-wall-clock/plugin-capabilities.md`.
- Current project map: `docs/STATE.md`.
- Decision log: `docs/DECISIONS.md`.
- Review answers and raw feedback: `docs/review/2026-08-11-wall-clock-round-1-answers.md` and `.context/review/2026-08-11-wall-clock-round-1.md`.
