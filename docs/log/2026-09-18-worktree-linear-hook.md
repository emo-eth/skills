# Worktree Linear hook

Slice 2 (not done). Slice 1 shipped `capture` only: bind or create from GOAL.md / `.herdr-ticket`, no `worktree.created` / `worktree.opened` events yet.

See `docs/log/2026-09-18-ticket-bound-slice-1-capture.md`.
